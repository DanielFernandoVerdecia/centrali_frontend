import { FechaNullPipe } from './fecha-null.pipe';

describe('FechaNullPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaNullPipe();
    expect(pipe).toBeTruthy();
  });
});
